diff --git a/node_modules/expo-av/android/CMakeLists.txt b/node_modules/expo-av/android/CMakeLists.txt
index e55dbb2..42d7eff 100644
--- a/node_modules/expo-av/android/CMakeLists.txt
+++ b/node_modules/expo-av/android/CMakeLists.txt
@@ -34,6 +34,14 @@ find_library(LOG_LIB log)
 
 find_package(ReactAndroid REQUIRED CONFIG)
 
+if (TARGET ReactAndroid::reactnativejni)
+        set(REACT_ANDROID_JNI_TARGET ReactAndroid::reactnativejni)
+elseif (TARGET ReactAndroid::reactnative)
+        set(REACT_ANDROID_JNI_TARGET ReactAndroid::reactnative)
+else ()
+        message(FATAL_ERROR "expo-av: ReactAndroid JNI target not found")
+endif ()
+
 find_package(fbjni REQUIRED CONFIG)
 
 # linking
@@ -43,6 +51,6 @@ target_link_libraries(
         ${LOG_LIB}
         fbjni::fbjni
         ReactAndroid::jsi
-        ReactAndroid::reactnativejni
+        ${REACT_ANDROID_JNI_TARGET}
         android
 )
